use super::element;

pub fn init() {
    // let wrapper = element::Element::create("div").unwrap();
    // let input = element::Element::create("input");
    // wrapper.inner.unwrap().append_child(&input.into());
    // element::Element::get_body().unwrap().append_child(&wrapper);
}
